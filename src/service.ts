import fs from 'node:fs';import path from 'node:path';import {candidates,config,normalize,periods,queryChanges,Store}from './core.js';import {ga,gsc,queryExamples}from './google.js';
export async function sync(refresh=false){const c=config(),store=new Store(c.dataDir),range=periods(c.lag),id=`${range.current.start}_${range.current.end}`,saved=store.get(id);if(saved&&!refresh)return {id,cached:true,...saved};const[a,b,gaNow,gaBefore]=await Promise.all([gsc(c,range.current),gsc(c,range.previous),ga(c,range.current),ga(c,range.previous)]);const enrich=(x:any[])=>x.map(r=>({...r,key:normalize(r.url,c)}));const snapshot={range,gsc:enrich(a.rows),previousGsc:enrich(b.rows),ga:enrich(gaNow),previousGa:enrich(gaBefore),coverage:{gscTruncated:a.truncated||b.truncated,notes:['GSC returns top rows; GA4 landing sessions are not query-attributed.']},createdAt:new Date().toISOString()};store.save(id,snapshot);return{id,cached:false,...snapshot}}
export async function pageContext(url:string,snapshotId?:string,limit=5){const c=config(),store=new Store(c.dataDir),r=snapshotId?store.get(snapshotId):await sync(),key=normalize(url,c),page=r.gsc.find((x:any)=>x.key===key)||r.previousGsc.find((x:any)=>x.key===key);if(!page)throw Error(`No page-level GSC row found for ${url} in this snapshot.`);const [current,previous]=await Promise.all([queryExamples(c,r.range.current,page.url,limit),queryExamples(c,r.range.previous,page.url,limit)]);return{snapshotId:snapshotId||r.id,url:page.url,periods:r.range,pagePerformance:{current:r.gsc.find((x:any)=>x.key===key)||null,previous:r.previousGsc.find((x:any)=>x.key===key)||null},acquisition:{current:r.ga.filter((x:any)=>x.key===key),previous:r.previousGa.filter((x:any)=>x.key===key)},queryEvidence:{current,previous,changes:queryChanges(current,previous),limitation:'Examples are limited top GSC rows, not complete query coverage; query evidence is not attributed to GA4 sessions.'}}}
export async function report(refresh=false){const r=await sync(refresh),c=config(),list=candidates(r.gsc,r.previousGsc);fs.mkdirSync(c.reportDir,{recursive:true,mode:0o700});const details=await Promise.all(list.slice(0,3).map(async x=>{try{return await pageContext(x.url,r.id,5)}catch(error){return{url:x.url,error:error instanceof Error?error.message:String(error)}}}));const lines=['# Site Signal report','',`Snapshot: \`${r.id}\` (${r.cached?'cached':'fresh'})`,'',`Periods: ${r.range.current.start}–${r.range.current.end} versus ${r.range.previous.start}–${r.range.previous.end}.`,'','## Evidence-backed investigations',''];if(!list.length)lines.push('No candidate met the configured review gates.');for(const [i,x]of list.slice(0,10).entries()){lines.push(`### ${x.url}`,'',`- Click change: ${x.clickChange}`,`- Impression change: ${x.impressionChange}`,`- Position change: ${x.positionChange.toFixed(1)}`);const detail=details[i]as any;if(detail?.queryEvidence?.changes?.length)lines.push('- Query examples with the largest observed movement:',...detail.queryEvidence.changes.slice(0,5).map((q:any)=>`  - ${q.query}: clicks ${q.previous.clicks} → ${q.current.clicks}; impressions ${q.previous.impressions} → ${q.current.impressions}; position ${q.baseline==='comparable'?`${q.previous.position.toFixed(1)} → ${q.current.position.toFixed(1)}`:'new/no baseline'}`));lines.push('- Limitation: query examples are top returned GSC rows, not complete coverage, and are not attributed to GA4 sessions.','')}const out=path.join(c.reportDir,`report-${r.id}.md`);fs.writeFileSync(out,lines.join('\n'));fs.writeFileSync(out.replace('.md','.json'),JSON.stringify({snapshotId:r.id,candidates:list,details},null,2));return{path:out,snapshotId:r.id,candidates:list.length}}
export async function status(){const c=config(),missing=['domain','gscProperty','ga4PropertyId'].filter(x=>!c[x as keyof typeof c]);return{configured:!missing.length,missingSetup:missing,dataDir:c.dataDir,capabilities:['read-only GSC page data','read-only GA4 landing sessions','local reports and snapshots','stdio MCP']}}
